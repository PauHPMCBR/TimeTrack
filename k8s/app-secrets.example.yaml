apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  # DO NOT commit real values. Prefer creating the secret with kubectl instead:
  #
  #   kubectl create secret generic app-secrets \
  #     --from-literal=mongodb-root-password='<openssl rand -hex 24>' \
  #     --from-literal=user-password='<openssl rand -hex 24>' \
  #     --from-literal=mongodb-uri='mongodb://alumne:<user-password>@mongodb:27017/myapp?authSource=myapp' \
  #     --from-literal=jwt-secret='<openssl rand -base64 48>'
  mongodb-root-password: "REPLACE_ME"
  user-password: "REPLACE_ME"
  mongodb-uri: "REPLACE_ME"
  jwt-secret: "REPLACE_ME"
